<!DOCTYPE html>
<html>
<head>
  <title>HackerNewsCloneRails</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	<div id="header"/>
  		<h3 id="logo">Y</h3>
  		<h3 id="site-title"><%= link_to "Hacker News", root_url %></h3>
  		<ul id="left-menu">
    		<li><%= link_to "new", new_post_path %></li>
        <% if current_user %>
          <li>|</li>
    		  <li><%= link_to "comments", user_comments_path(current_user.id) %></li>
        <% end %>
  		</ul>
  		<ul id="right-menu">
        <% if current_user %>
          <li>Logged in as: <%= link_to current_user.email, user_path(current_user.id) %> |</li>
  			   <li><%= link_to "Sign Out", signout_path %></li>
        <% else%>
          <li><%= link_to "Sign In", signin_path %> |</li>
          <li><%= link_to "Sign Up", signup_path %></li>
        <% end %>
  		</ul>
	</div>
	<div id="body-content">
		  <%= render 'layouts/notice' %>

	     <%= yield %>

	</div>

</body>
</html>
